{% extends 'base.html.twig' %}

{% block title %}Modifier la salle{% endblock %}

{% block body %}
    <h1>Modifier la salle</h1>

    <img src="/room/img/{{ room.photo }}" alt="{{ room.name}}" width="200">

    {{ include('room/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_room_index') }}">Retour à la liste</a>

    {{ include('room/_delete_form.html.twig') }}
{% endblock %}
