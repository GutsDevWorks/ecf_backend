<nav>
    <ul>
        <li>
            <a href="#">Accueil</a>
        </li>
        <li>
            <a href="{{ path('app_room_index') }}">Nos salles</a>
        </li>
        <li>
            <a href="{{ path('app_reservations_index') }}">Réservations</a>
        </li>

        {% if app.user %}
            {% if is_granted('ROLE_ADMIN') %}
                <li>
                    <a href="{{ path('admin_index') }}">Tableau de bord</a>
                </li>
            {% endif %}

            <li>
                <a href="{{ path('app_logout') }}">Mon Profil</a>
            </li>

            <li>
                <a href="{{ path('app_logout') }}">Se déconnecter</a>
            </li>
        {% else %}
            <li>
                <a href="{{ path('app_login') }}">Se connecter</a>
            </li>

            <li>
                <a href="{{ path('app_register') }}">S'inscrire</a>
            </li>
        {% endif %}
    </ul>
</nav>